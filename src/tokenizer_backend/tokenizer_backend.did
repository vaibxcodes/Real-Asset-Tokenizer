type UserProfile = record {
  principal: text;
  created_at: nat64;
};

service : {
  greet: (text) -> (text) query;
  create_or_get_profile: () -> (UserProfile);
  get_profile: (text) -> (opt UserProfile) query;
};
